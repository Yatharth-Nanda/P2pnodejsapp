const seeds = [
  { uri: "http://localhost:4000", user: "seed1" },
  { uri: "http://localhost:5000", user: "seed2" },
]; // HTTP routes here since we aren't configured for HTTPS yet
module.exports = { seeds };
